# Stage 12 — Clean deployment + IndexNow + authority

- Legacy `site.tar.gz` and `index.html.xz` are no longer used by Pages deployment.
- `_site` is assembled only from reviewed `overrides/` and `assets/`.
- Added IndexNow key `c25441bbac42b588dd51fcf237159c69` and automatic URL notification after successful deploy.
- Added Atom `feed.xml` and feed discovery link to HTML pages.
- Added Google/Yandex verification files directly to the clean static build.
- Added `GITHUB_PROFILE_README.md` for the public GitHub profile repository.
- Reworked root README into a clean indexable portfolio hub.
